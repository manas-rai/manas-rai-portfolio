# Never copy the host virtualenv into the image — it holds platform-specific
# binaries that would clobber the container's Linux venv built by `uv sync`.
.venv
venv

.git
.github
.gitignore

__pycache__
*.pyc
.pytest_cache
.ruff_cache

.env
.env.*
!.env.example

docs
tests
scratchpad
